# Python Runtime

Este diretorio reserva o local do venv Python embutido do app macOS.

O venv real deve ficar em:

```text
apps/macos/Resources/python/.venv
```

Ele e gerado por:

```bash
./scripts/bootstrap-embedded-venv.sh
```

O venv nao deve ser versionado.
